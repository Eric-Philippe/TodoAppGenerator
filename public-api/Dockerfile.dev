# Public API Development Dockerfile
FROM node:20-alpine

WORKDIR /app

# Installer les dépendances système
RUN apk add --no-cache curl

# Copier les fichiers de dépendances
COPY package*.json ./

# Créer un utilisateur non-root avant l'installation
RUN addgroup -g 1001 -S nodejs
RUN adduser -S nodejs -u 1001

# Installer toutes les dépendances (dev incluses)
RUN npm install

# Copier les fichiers restants avec le bon propriétaire
COPY --chown=nodejs:nodejs . .

# Passer à l'utilisateur nodejs
USER nodejs

# Exposer le port et le port de debug
EXPOSE 3001
EXPOSE 9229

# Commande de démarrage en mode développement (sans build)
CMD ["npm", "run", "dev"]
